Flask==2.3.2                   # Core Flask framework
gunicorn==20.1.0               # Production WSGI server for deployment
mysql-connector-python==8.0.33 # MySQL database connector
Jinja2==3.1.2                  # Template engine (usually installed with Flask)
Werkzeug==2.3.2                # WSGI utility library (installed with Flask)
itsdangerous==2.1.2            # Flask dependency for secure data
